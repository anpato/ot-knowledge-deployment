apiVersion: v1
kind: Service
metadata:
  name: ot-knowledge-mcp
  labels:
    app: ot-knowledge-mcp
spec:
  type: ClusterIP
  ports:
    - name: events
      port: 80
      targetPort: 3000
  selector:
    app: ot-knowledge-mcp
